# Cloudflare Pages 用（ルートに置く）
# デプロイ: npx wrangler pages deploy public
# ローカル: npx wrangler pages dev public --compatibility-flags=nodejs_compat

name = "simple-card-game"
pages_build_output_dir = "public"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]
